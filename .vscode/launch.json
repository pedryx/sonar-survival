{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "attach",
            "name": "Attach to running game",
            "pid": "${command:pickProcess}"
        }
    ]
}
